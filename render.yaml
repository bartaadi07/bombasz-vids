services:
  - type: web
    name: bombasz-vids
    env: node
    plan: free
    buildCommand: node setup-ytdlp.js
    startCommand: node server.js
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
